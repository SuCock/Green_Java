package programmer;

public class max {
	public static void main(String[] args) {
		int[] numbers = new int[6];
		int answer = 0;
		numbers = {0,31,24,10,1,9};
		
		// 배열안에 있는 숫자들을 하나하나 꺼내서 곱해준다(반복문)
		// 최댓값을 비교하면서 새로운 최댓값이 생길떄마다 덮어쓰기(조건문)
		
		
	}
}
